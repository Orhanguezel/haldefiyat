# Installed at /etc/nginx/snippets/haldefiyat-frontend-active.conf.
# scripts/frontend-blue-green.sh atomically changes only the server port.
upstream haldefiyat_frontend {
    server 127.0.0.1:3033;
    keepalive 8;
}
