-- Measurement columns are defined in 097 for fresh/repeatable seeds.
-- Existing pre-097 installations: run scripts/seo/competitor-recovery/migrate.ts.
-- Historical provenance stays NULL; never infer it from the selected engine.
SELECT 1;
